# Web Installer Firmware Folder

Place the release binary here as:

- `firmware.bin`

Expected final path:

- `docs/web-installer/releases/v2.0.0-dev/firmware.bin`

This path is referenced by `docs/web-installer/manifest.json`.
